<?xml version="1.0" encoding="utf-8"?>
<!-- 更新はアプリ側から明示的に行う（updatePeriodMillis=0）。
     数字を計算できるのはアプリだけなので、定期更新させても同じ値を貼り直すだけ。 -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="70dp"
    android:targetCellWidth="3"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_inbox"
    android:previewLayout="@layout/widget_inbox"
    android:description="@string/widget_inbox_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen" />
